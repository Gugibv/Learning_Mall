环境

```
nvm ls-remote
nvm install 22.14.0
nvm alias default v22.14.0
exec $SHELL

node -v 
v22.14.0
```

初始化项目

```
npm install -g @vue/cli    使用 Vue CLI 工具来创建项目
vue create mall-admin-web      
cd mall-admin-web
npm run serve              Vue 的开发服务器默认会运行在 http://localhost:8080
```

